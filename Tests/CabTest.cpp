#include <gtest/gtest.h>
#include "../Cab.h"

//our tests:
TEST(CabTests, SanityChecks) {
   /* Cab cab = Cab(10, HONDA, RED);
    ASSERT_EQ(cab.getId(),10);
    ASSERT_EQ(cab.getCarModel(),HONDA);
    ASSERT_EQ(cab.getColor(), RED);*/
}
